-First of all, i converted the file (CSV to JSON format) given in the appendix.
-To run the code, Simply open the folder in a code editor (My favourite code editor is VS Code).
-Run the commands "npm init" in the terminal and Navigate to the project folder(Web-app) then run the command "npm start" in the terminal

Additional topics:
-I didn't use any css library/framework(bootstrap) (instead of an inline style to look better the results)
-If i would have time. I could create a UI with a Navigation bar, having (a title, about, contacts, Videos, company & Cart links)
-As the main contents, I could use different bikeee bycycles with some paragraphs with some information.where people can not onle view but shop them.
-and then i end the layout with a nice footer. 